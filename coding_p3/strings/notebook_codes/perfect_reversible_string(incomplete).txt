#include <iostream>
#include<bits/stdc++.h>
using namespace std;
void substring(string str)
{
    int len=strlen(str);
    string st1,
    for(int j=1;j<=len;j++)
    {
        for(int i=0;i+j<=len;j++)
        {
            st1=substr(i,j);
            reverse(st1.begin(),st1.end());
            if(st.find(st1)==-1)
            {
                
            }
        }
    }
}

int main()
{
    string st;
    cin>>st;
    

}
